require.config({
  paths: {
    /* external libs */
    'jquery': '//cdnjs.cloudflare.com/ajax/libs/jquery/1.10.2/jquery.min',
    /* local paths */
    'template': '../template'
  }
});

require([], function() {
});
